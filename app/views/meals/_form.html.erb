<%= form_with(model: meal) do |form| %>
  <% if meal.errors.any? %>
    <div style="color: red">
      <h2><%= pluralize(meal.errors.count, "error") %> prohibited this meal from being saved:</h2>

      <ul>
        <% meal.errors.each do |error| %>
          <li><%= error.full_message %></li>
        <% end %>
      </ul>
    </div>
  <% end %>

  <div>
    <%= form.label :day, style: "display: block" %>
    <%= form.number_field :day %>
  </div>

  <div>
    <%= form.label :ordinal_number, style: "display: block" %>
    <%= form.number_field :ordinal_number %>
  </div>

  <div class="cursor-pointer">
    <%= form.label :date, style: "display: block" %>
    <%= form.date_field :date %>
  </div>

  <button class="bg-indigo-600 hover:bg-indigo-500 text-white rounded px-4 py-2">
    <%= form.submit %>
  </button>
<% end %>
